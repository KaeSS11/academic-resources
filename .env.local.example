# Admin password for accessing admin features
# Copy this file to .env.local and change the password
ADMIN_PASSWORD=your-secure-password-here

# Redis/KV Configuration (Required for production)
# Option 1: Vercel KV (recommended if deploying to Vercel)
# Get these from Vercel Dashboard → Storage → KV → Settings
# KV_REST_API_URL=https://your-kv-instance.upstash.io
# KV_REST_API_TOKEN=your-kv-token-here

# Option 2: Upstash Redis (if using Upstash directly)
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# Note: For local development, these are optional (code will use local file storage)
# For production on Vercel, you MUST set either KV_REST_API_URL/KV_REST_API_TOKEN 
# or UPSTASH_REDIS_REST_URL/UPSTASH_REDIS_REST_TOKEN
